# GCP Configuration
GCP_PROJECT_ID=your-gcp-project-id

# Path to GCP service account key (for local development)
# In Codespaces, this will be set automatically via secrets
GOOGLE_APPLICATION_CREDENTIALS=./server/gcp-key.json

# Server Configuration
PORT=8080

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# API Base URL (for frontend to connect to backend)
VITE_API_BASE_URL=http://localhost:8080/api

# ===========================================
# FOR GITHUB CODESPACES:
# ===========================================
# Set these as Codespace Secrets in your repo:
# Settings → Secrets and variables → Codespaces
# 
# Required Secrets:
# - GCP_PROJECT_ID: Your GCP project ID
# - GOOGLE_APPLICATION_CREDENTIALS: /workspaces/statshq/server/gcp-key.json
#
# Then upload your gcp-key.json file to /workspaces/statshq/server/
# The app will automatically detect Codespaces and use environment variables
